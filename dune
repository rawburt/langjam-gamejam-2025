(executable
 (name lgc)
 (preprocess
  (pps ppx_deriving.show))
 (libraries str)
 (modules
  lgc
  syntax
  grammar
  lexer
  emit
  type_check
  registry
  common
  analyzer
  name_resolution
  return_validation
  errors
  ty))

(menhir
 (modules grammar))

(ocamllex lexer)
