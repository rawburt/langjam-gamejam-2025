(executable
 (name lgc)
 (preprocess
  (pps ppx_deriving.show))
 (libraries str)
 (modules lgc syntax grammar lexer emit typecheck registry common analyzer))

(menhir
 (modules grammar))

(ocamllex lexer)
