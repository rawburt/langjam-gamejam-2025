use std

def rectfill2(x: int, y: int, w: int, h: int, c: color) do
  for xw = 0 to w do
    for yh = 0 to y do
      pset(x + xw, y + yh, c)
    end
  end
end

def draw() do
  rectfill2(40, 40, 50, 50, #9a0000)
end
